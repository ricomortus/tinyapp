<nav class="navbar navbar-expand-md navbar-dark bg-success">
  <a class="navbar-brand" href="/urls">TinyApp</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarNavAltMarkup"
    aria-controls="navbarNavAltMarkup"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav flex-grow-1">
      <a class="nav-item nav-link" href="/urls">My URLs</a>
      <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
    </div>
    <div class="login-reg">
      <% if (user && user.email) { %>
      <div class="d-flex">
          <span style="margin-right: 20px; margin-top: 8px">Logged in as: <%= user.email  %></span>
          <form action="/logout" method="post" style="display:inline;" >
            <button type="submit" class="btn btn-danger nav-item nav-link">Logout</button>
          </form>
      </div>
      <% } else { %>
        <div class="d-flex">
          <form action="/login" method="get">
            <button type="submit" class="btn" style="color:#a1d2a5">Login</button>
          </form>
          <form action="/register" method="get">
            <button type="submit" class="btn" style="color:#a1d2a5">Register</button>
          </form>
        </div>
      <% } %>
    </div>
  </div>
</nav>
